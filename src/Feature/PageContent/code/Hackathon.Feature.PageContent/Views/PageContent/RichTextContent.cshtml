@model Hackathon.Feature.PageContent.Models.ViewModels.RichTextContentViewModel

@if (Model != null)
{
    <div id="fh5co-intro-section">
        <div class="container">
            <div class="row">
                <div class="col-md-8 col-md-offset-2 animate-box text-center">
                    @Html.Raw(Model.TextContent)
                </div>
            </div>
        </div>
    </div>
}